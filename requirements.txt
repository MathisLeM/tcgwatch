# ============================================================================
# TCGWatch — Python dependencies (scraper + API + worker)
# Mirrors the Vigilyx stack. Install:  pip install -r requirements.txt
# ============================================================================

# ---- Scraper ----------------------------------------------------------------
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pandas>=2.2.0
openpyxl>=3.1.0

# ---- Legacy local dashboard (Streamlit) — dev only, not deployed ------------
streamlit>=1.30.0

# ---- API backend (FastAPI on Railway) ---------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # OAuth2PasswordRequestForm (login form)

# ---- Database / ORM / migrations --------------------------------------------
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9           # PostgreSQL driver (Supabase, prod)

# ---- Config / validation ----------------------------------------------------
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0           # EmailStr validation

# ---- Auth -------------------------------------------------------------------
python-jose[cryptography]>=3.3.0 # JWT
bcrypt>=4.1.0                     # password hashing

# ---- Rate limiting ----------------------------------------------------------
slowapi>=0.1.9

# ---- Scheduling (scraper worker) --------------------------------------------
apscheduler>=3.10.0

# ---- Object storage (Cloudflare R2, S3-compatible) --------------------------
boto3>=1.34.0

# ---- Tests ------------------------------------------------------------------
pytest>=8.0.0
httpx>=0.27.0                    # FastAPI TestClient transport
